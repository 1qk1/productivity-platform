import React from "react";

import "./Hero.scss";

const hero = () => (
  <section className="Section Hero">
    <div className="Container">
      <h1>I don't even know</h1>
    </div>
  </section>
);

export default hero;
